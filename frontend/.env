# Apunta el boton "Obtener Token y Enviar al Backend" (TokenSender.jsx)
# directo a tu auth-api local en vez de la URL de produccion en AWS.
# Cambialo por catalogo-api (8081) o carrito-api (8082) si quieres probar
# esos en vez del auth-api.
VITE_API_URL=http://localhost:8083/api/v1/auth/me
